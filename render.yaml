services:
  # ─── 1. NestJS Backend API ──────────────────────────────
  - type: web
    name: gigpilot-backend
    env: node
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: npm install --include=dev && npx prisma generate && npm run build
    startCommand: npx prisma db push && node dist/main.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: gigpilot-db
          property: connectionString
      - key: REDIS_HOST
        fromService:
          type: redis
          name: gigpilot-redis
          property: host
      - key: REDIS_PORT
        fromService:
          type: redis
          name: gigpilot-redis
          property: port
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: PORT
        value: 3001

  # ─── 2. Next.js Web Dashboard Frontend ───────────────────
  - type: web
    name: gigpilot-frontend
    env: node
    region: oregon
    plan: free
    rootDir: frontend
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm run start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: gigpilot-backend
          property: host

  # ─── 3. Redis Queue / Cache Service ─────────────────────
  - type: redis
    name: gigpilot-redis
    region: oregon
    plan: free
    ipAllowList: [] # internal access only

databases:
  # ─── 4. PostgreSQL Database ──────────────────────────────
  - name: gigpilot-db
    region: oregon
    plan: free
    databaseName: gigpilot_ai
    user: gigpilot
